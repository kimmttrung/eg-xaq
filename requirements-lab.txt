# Nối vào mô hình + raster của lab. CHỈ cài khi đã có checkpoint.
# ⚠ Phiên bản xgboost/scikit-learn phải KHỚP với phiên bản lúc lab pickle model,
#   nếu không sẽ không load được .pkl (xem docs/02-data-contract.md M2).
-r requirements.txt
xgboost>=2.0
scikit-learn>=1.4
shap>=0.45
joblib>=1.4
xarray>=2024.3
netcdf4>=1.6
rasterio>=1.3
cfgrib>=0.9
geopandas>=0.14
shapely>=2.0
